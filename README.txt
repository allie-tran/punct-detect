To run the model, run "punct_detect.py" in "punct-detect" folder.
To get the results, run "Fscore.py" in the same folder. Then check the results in "result" folder.
Currently, the dataset used is from data/news